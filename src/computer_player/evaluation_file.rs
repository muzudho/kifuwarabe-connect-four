use crate::computer_player::Evaluation;

impl Evaluation {
    /// Save to a file.  
    /// TODO ファイルへ保存します。  
    pub fn save(&self) {
        // Open the file.

        // Write.
    }

    /// Load from a file.  
    /// TODO ファイルへ保存します。  
    pub fn load(&self) {}
}
